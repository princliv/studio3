import 'package:flutter/material.dart';

/// Reserved slot between Featured and Feed for a future "Happening Near You"
/// horizontal event carousel (Figma 2426-1678). Not rendered yet.
class ExploreNearYouPlaceholder extends StatelessWidget {
  const ExploreNearYouPlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
